# 复制为 .env 后按需填写（全部可选）。.env 已在 .gitignore 里，不会提交。
# 这些只在本机运行 npm start / npm run arena 时使用；网页里填的 key 优先。

# Laya 服务（Jev 兼容接口）：给服务根地址即可，接口路径 /v1/systemone 自动拼接
# LAYA_BASE_URL=http://192.168.x.x:8790
# LAYA_API_KEY=你的_laya_密钥
# LAYA_MODEL=laya                    # laya / laya-multilingual / laya-typed-decisions；留空则自动选

# Jev（TypeSafe 云端）：网页没填 key 时用这个
# JEV_API_KEY=你的_jev_密钥
# JEV_MODEL=jev-latest
BUDGET_USD=1                         # 用上面这个 Jev key 时的累计花费上限（美元）

# 允许调用本地中转的网页来源（逗号分隔），默认 https://wubugui.github.io
# CORS_ORIGINS=https://wubugui.github.io

# PORT=3000
# DECISION_LOG=0   # 关闭决策日志
